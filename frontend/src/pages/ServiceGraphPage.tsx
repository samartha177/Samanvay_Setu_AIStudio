import { PageHeader } from "../components/PageHeader";
import { PlaceholderPanel } from "../components/PlaceholderPanel";

export function ServiceGraphPage() {
  return <><PageHeader eyebrow="Officer console" title="Service dependency graph" description="Make the scholarship service's department dependencies visible and understandable." /><PlaceholderPanel phase="Phase 5" heading="React Flow service graph" description="This route will render the identity, education, income, gateway, and scholarship dependencies using live workflow state." /></>;
}
