{
  "name": "samanvaysetu",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "start": "node server.ts",
    "build": "npm --prefix frontend run build && rm -rf dist && cp -r frontend/dist dist",
    "preview": "npm --prefix frontend run preview",
    "lint": "npm --prefix frontend run build",
    "test": "bun test frontend/src/tests/"
  },
  "dependencies": {
    "@google/genai": "^2.21.0"
  }
}
